model_panel <- tabPanel("Modelisation", icon = icon("bar-chart"),
                        sidebarLayout(
                          sidebarPanel(
                            align = "center",
                            br(),
                            br()
                          ),
                          mainPanel(
                            h4("Soon available"),
                            br()
                          )
                        )
)